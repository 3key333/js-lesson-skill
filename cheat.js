// ====== МЕТОДЫ======
строка.replace(чтo_мeняeм, нa_чтo_мeняeм)

       Паттерн	  Что значит	                    Пример
//     /а/g	      все буквы "а"	                  "банан" → "бннн"
//     /а/gi	    все "а" без учета регистра      "БАнАн" → "Бннн"
//     /\d/g	    все цифры	                      "Цена: 100$" → "Цена: $"
//     /\D/g	    НЕ цифры	                      "Тел: +7(999)123" → "7999123"
//     /\s/g	    все пробелы	                    "a b c" → "abc"
//     /\S/g	    НЕ пробелы	                    "a b c" → "a b c"
//     /./g	      любой символ	                  "abc" → "xxx"

1. Основные.для.работы:

// ДОБАВЛЕНИЕ/УДАЛЕНИЕ
arr.push(element)      // добавить в конец
arr.pop()              // удалить последний
arr.unshift(element)   // добавить в начало  
arr.shift()            // удалить первый

// ПЕРЕБОР
arr.forEach(func)      // для каждого элемента
arr.map(func)          // преобразовать все элементы
arr.filter(func)       // отфильтровать по условию
arr.find(func)         // найти первый подходящий
arr.includes(element)  // проверить наличие

// ПРЕОБРАЗОВАНИЕ
arr.join(separator)    // объединить в строку
arr.concat(arr2)       // объединить массивы
arr.slice(start, end)  // вырезать часть
arr.splice(start, del, ...items) // удалить/вставить

// ПОИСК
arr.indexOf(element)   // найти индекс элемента
arr.lastIndexOf(element) // найти с конца

// СОРТИРОВКА
arr.sort()             // отсортировать
arr.reverse()          // перевернуть

// ПРОВЕРКА
arr.every(func)        // все элементы удовлетворяют?
arr.some(func)         // хотя бы один удовлетворяет?

// СВОРАЧИВАНИЕ
arr.reduce(func, initial) // "свернуть" массив в одно значение

2. Самые важные (выучить в первую очередь):
javascript
// 1. push/pop - работа с концом массива
let arr = [1, 2, 3];
arr.push(4);    // [1, 2, 3, 4]
arr.pop();      // [1, 2, 3]

// 2. forEach - перебор всех элементов
arr.forEach(item => console.log(item));

// 3. map - преобразовать все элементы
let doubled = arr.map(x => x * 2); // [2, 4, 6]

// 4. filter - отфильтровать
let even = arr.filter(x => x % 2 === 0); // [2]

// 5. find - найти элемент
let found = arr.find(x => x > 1); // 2

// 6. includes - проверить наличие
arr.includes(2); // true

// 7. join - массив в строку
arr.join(', '); // "1, 2, 3"

// 8. slice - вырезать часть
arr.slice(1, 3); // [2, 3] (с 1 индекса до 3 не включительно)

СТРОКИ (String)
1. Основные для работы:

javascript
// ДЛИНА И ДОСТУП
str.length             // длина строки
str[index]             // символ по индексу
str.charAt(index)      // символ по индексу (старый способ)

// РЕГИСТР
str.toUpperCase()      // в ВЕРХНИЙ регистр
str.toLowerCase()      // в нижний регистр

// ПОИСК
str.includes(substr)   // содержит подстроку?
str.indexOf(substr)    // индекс начала подстроки
str.lastIndexOf(substr) // поиск с конца
str.startsWith(substr) // начинается с?
str.endsWith(substr)   // заканчивается на?

// ИЗМЕНЕНИЕ
str.replace(old, new)  // заменить
str.trim()             // убрать пробелы с краёв
str.slice(start, end)  // вырезать часть
str.substring(start, end) // почти как slice
str.split(separator)   // разбить на массив

// ПОВТОРЕНИЕ
str.repeat(count)      // повторить строку
2. Самые важные (выучить в первую очередь):

let str = "Hello World";

// 1. length - длина
str.length; // 11

// 2. toUpperCase/toLowerCase - регистр
str.toUpperCase(); // "HELLO WORLD"
str.toLowerCase(); // "hello world"

// 3. includes - содержит ли
str.includes("World"); // true

// 4. indexOf - найти позицию
str.indexOf("o"); // 4 (первая 'o')
str.lastIndexOf("o"); // 7 (последняя 'o')

// 5. slice - вырезать часть
str.slice(0, 5); // "Hello"
str.slice(6); // "World"

// 6. replace - заменить
str.replace("World", "JavaScript"); // "Hello JavaScript"

// 7. split - разбить на массив
str.split(" "); // ["Hello", "World"]
"a,b,c".split(","); // ["a", "b", "c"]

// 8. trim - убрать пробелы
"  hello  ".trim(); // "hello"

🏷️ ОБЪЕКТЫ (Object)
1. Основные для работы:
// СОЗДАНИЕ/ДОСТУП
obj.key = value        // добавить/изменить свойство
obj['key'] = value     // то же через []
delete obj.key         // удалить свойство

// ПРОВЕРКА
'key' in obj           // есть свойство?
obj.hasOwnProperty('key') // есть собственное свойство?

// ПЕРЕБОР
Object.keys(obj)       // массив ключей
Object.values(obj)     // массив значений  
Object.entries(obj)    // массив пар [ключ, значение]

// КОПИРОВАНИЕ
Object.assign({}, obj) // копировать объект
{...obj}               // spread оператор (копия)

// ЗАМОРОЗКА
Object.freeze(obj)     // сделать объект неизменяемым
Object.seal(obj)       // запретить добавлять/удалять свойства

2. Самые важные (выучить в первую очередь):


let user = {name: "John", age: 30, city: "NY"};

// 1. Доступ к свойствам
user.name; // "John" (точка)
user["age"]; // 30 (скобки)

// 2. Object.keys - все ключи
Object.keys(user); // ["name", "age", "city"]

// 3. Object.values - все значения
Object.values(user); // ["John", 30, "NY"]

// 4. Object.entries - пары ключ-значение
Object.entries(user); // [["name", "John"], ["age", 30], ["city", "NY"]]

// 5. Проверка свойства
"name" in user; // true
user.hasOwnProperty("age"); // true

// 6. Копирование объекта
let userCopy = {...user}; // или Object.assign({}, user)

// 7. Удаление свойства
delete user.city;

🔢 ЧИСЛА (Number)
Обязательные:

// ПРЕОБРАЗОВАНИЕ
Number(str)           // строка → число
parseInt(str)         // строка → целое число  
parseFloat(str)       // строка → дробное число
num.toString()        // число → строка

// ПРОВЕРКИ
isNaN(value)          // Not a Number?
Number.isInteger(num) // целое число?
Number.isFinite(num)  // конечное число?

// ОКРУГЛЕНИЕ
Math.round(num)       // округлить
Math.floor(num)       // округлить вниз
Math.ceil(num)        // округлить вверх
Math.trunc(num)       // отбросить дробную частьсщтыщдуюдщ

// МАТЕМАТИКА
Math.abs(num)         // модуль числа
Math.max(a, b, c)     // максимум
Math.min(a, b, c)     // минимум
Math.pow(base, exp)   // степень
Math.sqrt(num)        // квадратный корень
Math.random()         // случайное число 0-1

//ДАТЫ (Date)
new Date()                    // текущая дата/время
Date.now()                    // timestamp (мс с 1970)

let date = new Date();

date.getFullYear()            // год (4 цифры)
date.getMonth()               // месяц (0-11!)
date.getDate()                // день месяца (1-31)
date.getDay()                 // день недели (0-6)
date.getHours()               // часы (0-23)
date.getMinutes()             // минуты
date.getSeconds()             // секунды
date.getTime()                // timestamp

date.toDateString()           // дата в читаемом виде
date.toTimeString()           // время в читаемом виде
date.toLocaleDateString()     // локализованная дата




// ====== РАБОТА С ЧИСЛАМИ ======
// Проверка положительное/отрицательное
if (num > 0) {}
if (num < 0) {}
Math.abs(num) // модуль


// ====== РАБОТА СО СТРОКАМИ ======
str.length // длина
str[i] // символ по индексу
str.toUpperCase() // в верхний регистр
str.toLowerCase() // в нижний регистр
str.includes("текст") // содержит ли
str.split(" ") // разбить по пробелу в массив


// ====== ЦИКЛЫ ======
while( условие ) {
  // тело цикла
}

do {
    // тело цикла
} while( условие )

for(начальное.значение; условие; шаг.цикла) {
    // тело цикла
}

{continuе;}  // Цикл сразу переходит на следующую итерацию. Код, который находится после данной команды, не запускается.
{breaк;} // Цикл полностью останавливает работу и начинает выполняться код, находящийся после цикла.


// ====== РАБОТА С МАССИВАМИ ======
arr.length // длина массива
arr[i] // элемент по индексу
arr.push(element) // добавить в конец
arr.pop() // удалить последний
arr.shift() // удалить первый
arr.includes(item) // содержит ли
arr.indexOf(item) // содержет ли (вернет индекс элемента)


// Циклы по массиву:
for (let i = 0; i < arr.length; i++) {
  console.log(arr[i]);
}


// ====== РАБОТА С ОБЪЕКТАМИ ======
obj.key // доступ по ключу
obj["key"] // альтернативный доступ (если название ключа это 2 слова то пишем только в данном типе)
obj.some = 'hello' // добавление ключа в объект
delete obj.some // удаление ключа (обращение к ключу может быть разного типа )
Object.keys(obj) // массив ключей
Object.values(obj) // массив значений

// Цикл по объекту:
for (let key in obj) {
  console.log(key, obj[key]);
}


// ====== MAP и SET ======

//map
const map = new Map(); // создали пустую коллекцию
const map1 = new Map([['ключ', значение], ['second', 2]]);

set(ключ, значение) //устанавливает значение;
get(ключ) //возвращает значение;
has(ключ) //проверяет наличие переданного ключа;
values() //возвращает итерируемый объект всех значений коллекции;
keys() //возвращает итерируемый объект всех ключей коллекции;
entries() //возвращает итерируемый объект пар [ключ, значение];
delete(ключ) //удаляет конкретное значение;
clear() //полностью очищает коллекцию;
forEach(колбэк) //перебирает ключи и значения коллекции;
size //показывает количество элементов в коллекции. Работает так же, как свойство length.

//set
const setCollection1 = new Set(); // создали пустую коллекцию
const setCollection2 = new Set(['string', 1,  true, 'any']);

add() //добавить элемент.
delete(el) //удалить элемент.
has() //проверить, есть ли элемент в коллекции.
clear() //очистить коллекцию.
size //показывает количество элементов в коллекции, работает так же, как свойство length.

Проходить.по.элементам.коллекции.можно.с.помощью.цикла.for.of

for (let elem of setCollection2) {
    console.log(elem); // string, 1, true, any 
}


// ====== ОБЩИЕ ПРИНЦИПЫ ======

// Функция:

function имяФункции(параметр) {
  // код
  return результат;
}

// стрелочная
(аргументы) => { тело.функции }




// Условия:
if (условие)
  // если true
} else {
  // если false
}